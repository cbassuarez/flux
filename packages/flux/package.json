{
  "name": "@flux-lang/flux",
  "version": "0.1.30",
  "description": "Global Flux launcher that keeps @flux-lang/cli up to date and cached",
  "license": "MIT",
  "author": "Sebastian Suarez-Solis",
  "type": "module",
  "private": false,
  "bin": {
    "flux": "dist/bin/flux.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "prepublishOnly": "npm run build && chmod +x dist/bin/flux.js",
    "test": "echo \"(no tests for flux launcher)\""
  },
  "dependencies": {
    "env-paths": "^3.0.0",
    "semver": "^7.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/semver": "^7.7.1",
    "typescript": "^5.6.0"
  }
}
