link-workspace-packages=true
prefer-workspace-packages=true
# Hoist @types/* to the root so tsc resolves Node built-in types in packages
# that use them (typesetter, render-html, viewer) without each declaring
# @types/node. Without this, pnpm's strict node_modules breaks the build in CI.
public-hoist-pattern[]=*@types/*
